# Frontend (Next.js)

## Setup

```bash
cd frontend
npm install
```

Create `.env.local`:

```bash
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
```

## Run

```bash
npm run dev
```

The app expects the FastAPI backend to be running.
